/**
 * Placeholder tab view for the Weekly Planner (weekly-planner spec fills this in).
 * Intentionally minimal: the platform foundation only establishes the shell and
 * routing; feature content lands in later specs.
 */
export function WeeklyPlanner(): JSX.Element {
  return (
    <section aria-labelledby="weekly-planner-heading">
      <h1 id="weekly-planner-heading">Weekly Planner</h1>
      <p>The weekly meal planner will appear here.</p>
    </section>
  );
}
