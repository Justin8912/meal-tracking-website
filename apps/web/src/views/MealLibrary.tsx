/**
 * Placeholder tab view for the Meal Library (recipe-library spec fills this in).
 * Intentionally minimal: the platform foundation only establishes the shell and
 * routing; feature content lands in later specs.
 */
export function MealLibrary(): JSX.Element {
  return (
    <section aria-labelledby="meal-library-heading">
      <h1 id="meal-library-heading">Meal Library</h1>
      <p>The recipe library will appear here.</p>
    </section>
  );
}
